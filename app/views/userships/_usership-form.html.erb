 <%= form_with(model: usership) do |forma| %>
  <% if usership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usership.errors.count, "error") %> prohibited this usership from being saved:</h2>

      <ul>
        <% usership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div>
     <%#= forma.select :ship_id, options_for_select(@userships.collect{ |u| [u.model, u.id] } ), :include_blank => 'Select from My Ships' %>
  <%= forma.select :ship_id, options_for_select(@ships.collect{ |u| [u.model, u.id] } ), :include_blank => 'Select Ship' %>
  <%= forma.label "Default Ship" %>
  <%= forma.check_box :primary %>
  </div>
  <%= forma.submit %>
<% end %>
  

  