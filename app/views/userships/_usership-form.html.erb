 <%= form_with(model: usership, :html => {:id => 'userships_form', :style => 'display:none;' }) do |forma| %>
  <% if usership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usership.errors.count, "error") %> prohibited this usership from being saved:</h2>

      <ul>
        <% usership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field" style="margin-top:5px; margin-bottom:10px;">

  <%= forma.select :ship_id, options_for_select(@ships.collect{ |u| [u.model, u.id] } ), :include_blank => 'Select New Ship' %>
  <%= forma.label "Primary Ship" %>
  <%= forma.check_box :primary %>

  <%= forma.submit "Add ship to hangar"  %>
<%= button_tag t('buttons.cancel'), type: "submit", name: "cancel", value: true %>
    </div>
<% end %>
  

  