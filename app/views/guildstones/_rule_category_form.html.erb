 <%= form_with(model: category, :html => {:id => 'rule_category_form', :style => 'display:none;' }) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<span class="testingcat">
  New Category: <%= form.text_field :name %>
  </span>
  <%= form.submit "Create new category", id: 'category_submit'  %>

<%#= button_tag t('buttons.cancel'), type: "submit", name: "cancel", class:'cta red', value: true %>
<% end %>