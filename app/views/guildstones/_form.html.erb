<%= form_with(model: guildstone) do |form| %>
  <% if guildstone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(guildstone.errors.count, "error") %> prohibited this guildstone from being saved:</h2>

      <ul>
        <% guildstone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :charter, style: "display: block" %>
    <%= form.text_field :charter %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
