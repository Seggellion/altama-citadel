<%= form_with(model: event_team) do |form| %>
  <% if event_team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event_team.errors.count, "error") %> prohibited this event_team from being saved:</h2>

      <ul>
        <% event_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :event_id, style: "display: block" %>
    <%= form.number_field :event_id %>
  </div>

  <div>
    <%= form.label :team_id, style: "display: block" %>
    <%= form.number_field :team_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
