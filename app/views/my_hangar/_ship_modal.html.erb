<div id="ship_modal" class="">
    <div class="ship-modal-container">
    <div class="ship-modal-content-container">
    <h2 class="ship-modal-header"><%= usership.ship_name %></h2>
    <h3 class="ship-modal-subheader"><%= usership.ship.manufacturer.name %> - <%= usership.ship.model %></h3>

             <%= image_tag(usership.ship.ship_image_primary) if File.file?('app/assets/images/' + usership.ship.ship_image_primary) %>
    </div>
                <%= link_to " ", close_state_window_path(window: "modal-#{usership.id}" , task: @current_task), class:'close-btn windowed' %>
    </div>
</div>
<script>
document.getElementsByClassName('fleet-name')[0].style.display = "none";
</script>