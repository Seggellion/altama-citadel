<%= form_with(model: rule_proposal) do |form| %>
  <% if rule_proposal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rule_proposal.errors.count, "error") %> prohibited this rule_proposal from being saved:</h2>

      <ul>
        <% rule_proposal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
