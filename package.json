{
  "name": "app",
  "private": "true",
  "type": "module",
  "dependencies": {
    "chart.js": "^4.3.0",
    "esbuild": "^0.14.13"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
