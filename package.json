{
  "name": "app",
  "private": "true",
  "type": "module",
  "dependencies": {
    "esbuild": "^0.14.13",
    "stimulus-autocomplete": "^3.1.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
